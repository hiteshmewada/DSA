#include<bits/stdc++.h>
using namespace std;
int main(){
    cout<<"Enter a number ";
    
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
    int n,a;
    cin>>n>>a;
    cout<<" "<<n+a;
    

    return 0;
}
/* Download mingw and then download some extensions from VS Code 
Check versions and if not present check in environment variables
and then edit configurations in c/c++ file
and then to compile the code 
commands are g++ filename
    ./a
    For CP, install competitive companion extension
    Now for file handling we have extensions and boiler plate
    now go to settings -> extensions -> Run Code Configurations -> Edit in text json
*/